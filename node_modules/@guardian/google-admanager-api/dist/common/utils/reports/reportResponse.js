"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
Object.defineProperty(exports, "__esModule", { value: true });
exports.ReportResponse = void 0;
const fs = __importStar(require("fs"));
const stream = __importStar(require("stream"));
const util_1 = require("util");
const handlers_1 = require("../../handlers");
/**
 * Represents a report response from the server.
 */
class ReportResponse {
    constructor(response) {
        if (response == null) {
            throw new handlers_1.ArgumentNullException("Response cannot be null.");
        }
        this.response = response;
    }
    /**
     * Saves the report to a specified path
     *
     * @param path The path to which report is saved.
     */
    async save(path) {
        this.ensureStreamIsOpen();
        const finishedDownload = (0, util_1.promisify)(stream.finished);
        const writer = fs.createWriteStream(path);
        try {
            this.response.data.pipe(writer);
            await finishedDownload(writer);
        }
        catch (err) {
            throw new handlers_1.AdsReportsException("Failed to save report. See inner exception for more details.", err);
        }
    }
    /**
     * Checks to ensure that the underlying stream has not been closed.
     */
    ensureStreamIsOpen() {
        if (this.response == null) {
            throw new handlers_1.AdsReportsException("Cannot access a closed report response stream.");
        }
    }
}
exports.ReportResponse = ReportResponse;
//# sourceMappingURL=reportResponse.js.map